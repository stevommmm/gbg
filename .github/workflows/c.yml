name: C/C++ CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: sudo apt install --no-install-recommends -qq -y libimlib2-dev libx11-dev
    - name: make
      run: make
